# 曦码输入法.az v2.6.2 发行说明

> 本版本基于 [Xime](https://github.com/ximeiorg/Xime) v2.6.2 修改，包名为 `com.kingzcheung.xime.az`。
> 对应构建：[Build and Test APK #9](https://github.com/AZNixl/Xime.az/actions/runs/32920024799)

## 相较原版 Xime 新增/修改功能

### 键盘符号与手势自定义
- **键盘符号编辑**：设置页可直接编辑字母键、逗号键、句号键、空格键、?123 键的点按/上滑/下滑/长按手势。
- **?123 键（mode_change）可配置**：支持自定义 tap/上滑/下滑/长按；未配置时保持默认行为（点按切换面板，长按弹气泡选择九宫格数字键盘或常用符号键盘）。
- **临时候选键**：句号键、?123 键等支持在有编码时动态显示为第二候选/第三候选/取消编码，与打字流程深度整合。
- **26 字母键长按符号全部显示**：所有字母键的长按首项符号都会显示在键帽左上角（AXCV 显示全选/剪切/复制/粘贴，其余显示对应字母/符号）。

### 输入体验优化
- **空格键长按连续输入空格**：默认长按空格像退格键一样连续输入空格；切换中英文可作为用户自定义动作保留。
- **中文区 qwerty 下滑改为常用符号直接上屏**：q… w· e× r【 t】 y—— u「 i」 o《 p》 a℃ s§ d÷ f± g★ h☆ j√ k∞ l° z※ x→ c← v↑ b↓ n≈ m≠。
- **中英键盘 A/X/C/V 长按新增快捷键**：全选 / 剪切 / 复制 / 粘贴（原字母选项保留在后）。
- **退格键手势精简**：移除滑动选中删除，仅保留点删、上滑清空、下滑撤回。
- **光标移动手势迁移到候选栏**：空格键滑动不再移动光标。
- **空格键去除中/英角标**：支持在设置页自定义显示文本。

### 布局与个性化
- **键盘高度逐行可调**：第 1~4 行高度可单独调节，新增可选「增高行」（纯空白第五行）。
- **九宫格数字键盘接入增高行**：横屏/竖屏数字键盘均跟随主键盘的增高行设置。
- **丰富的配色与背景主题**：内置多套纯色、渐变、图片背景主题。
- **自定义键盘字体**：支持将 ttf/otf 字体放入 `/Documents/Xime/fonts/` 使用。
- **密码框内英文直出**：不上屏联想，设置页有开关。

### 配置与同步
- **xime.yaml 外置**：优先读取 `/Documents/Xime/rime/xime.yaml`，不存在时回退 assets 内置版本，方便用户无需重新打包即可调整配置。
- **WebDAV 同步范围扩展**：同步整个 `/Documents/Xime/` 目录（跳过 fonts/build）。
- **设置项持久化与覆盖**：用户通过设置页修改的手势以最高优先级覆盖默认配置。

## 安装包

从本次 Release 的 Assets 中下载 APK：
- `app-debug.apk`：调试版本（可直接安装测试）

> 注意：当前 workflow 输出的是 Debug APK。如需 Release 签名 APK，请提供签名配置或手动打包。

## 适用版本

- 最低 Android 版本：与原版 Xime 一致
- 包名：`com.kingzcheung.xime.az`
