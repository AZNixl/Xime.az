<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".PluginDeclaration"
            android:exported="true">
            <intent-filter>
                <action android:name="com.kingzcheung.xime.plugin.EXTENSION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="plugin.entryClass"
            android:value="com.kingzcheung.xime.plugin.funasr.FunAsrPlugin" />

        <meta-data
            android:name="plugin.description"
            android:value="阿里百炼 FunAsr 在线语音识别" />

        <meta-data
            android:name="plugin.type"
            android:value="speech" />

        <meta-data
            android:name="plugin.minHostVersion"
            android:value="2.6.0" />

    </application>

</manifest>
